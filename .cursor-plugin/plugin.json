{
  "name": "botrelay",
  "displayName": "BotRelay",
  "version": "0.3.1",
  "description": "A password manager built for AI agents. Hosted MCP returns sealed secrets; botrelay-cli decrypts them on your machine. Give agents their own vault, only the secrets they need, and tools that keep the vault key local. Visit https://botrelay.ai for more information.",
  "author": {
    "name": "BotRelay"
  },
  "homepage": "https://botrelay.ai",
  "repository": "https://github.com/botrelay-ai/botrelay-plugin",
  "keywords": [
    "botrelay",
    "vault",
    "secrets",
    "passwords",
    "zero-knowledge",
    "login",
    "credentials",
    "security"
  ],
  "category": "integrations",
  "tags": [
    "passwords",
    "secrets",
    "vault",
    "mcp"
  ],
  "logo": "assets/botrelay-logo-marketplace.png",
  "skills": "./skills/",
  "rules": "./rules/",
  "commands": "./commands/",
  "mcpServers": "./mcp.json",
  "variables": {
    "type": "object",
    "properties": {
      "API_BASE_URL": {
        "type": "string",
        "title": "API base URL",
        "description": "Base URL of BotRelay API. Do not include /mcp/ in this value.",
        "default": "https://api.botrelay.ai"
      },
      "BOTRELAY_API_KEY": {
        "type": "string",
        "title": "API key",
        "description": "The API key (brt_live_…) generated during vault creation." 
      }
    },
    "required": ["BOTRELAY_API_KEY"]
  },
  "license": "MIT"
}
